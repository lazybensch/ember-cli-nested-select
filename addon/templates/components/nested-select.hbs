{{#unless listVisible}}
  {{input value=selected.label readonly=true action='showList' on='focus-in' classNames='nested-select-text'}}
{{else}}
  {{input value=query action='quickSelect' on='insert-newline' classNames='keep-nested-select nested-select-text'}}
  <label class='preview nested-select-text'> {{preview}} </label>
{{/unless}}

{{#if (and selected showRevert)}}
  <i class='revert keep-nested-select nested-select-text' {{action 'revert'}}>{{fa-icon 'times'}}</i>
{{/if}}

<ol class='nested-select-list' hidden={{if listVisible false true}}>
  {{#each filteredContent as |entry|}}
    <li class='nested-select-text {{if entry.children 'keep-nested-select'}}' {{action 'select' entry on='click'}}>
      <label>
        {{entry.label}}
      </label>
      {{#if entry.children}}
        <i class='next fa fa-chevron-right'></i>
      {{/if}}
    </li>
  {{else}}
    <li>
      nothing found for '{{query}}'
    </li>
  {{/each}}
</ol>
