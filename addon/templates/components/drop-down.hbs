{{input value=selected.label readonly=true}}

<i>s</i>
{{input value=query}}
<label> {{preview}} </label>
<i {{action 'revert'}}>clear</i>

<ol class='list'>
  {{#each filteredContent as |entry|}}
    <li {{action 'select' entry on='click'}}>{{entry.label}}</li>
  {{else}}
    <li>nothing found for '{{query}}'</li>
  {{/each}}
</ol>
